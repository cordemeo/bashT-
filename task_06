#!/bin/bash
# вывод всех исполняемых файлов в директории для которых у текущего пользователя есть права
set -e

echo "Список исполняемых файлов в директории для которых у текущего пользователя есть права: "
for elem in *; do
    if [ -x $elem ]; then
        echo " $elem "
    fi
done       
